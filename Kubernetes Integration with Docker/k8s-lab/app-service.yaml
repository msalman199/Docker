apiVersion: v1
kind: Service
metadata:
  name: k8s-lab-app-service
spec:
  selector:
    app: k8s-lab-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
